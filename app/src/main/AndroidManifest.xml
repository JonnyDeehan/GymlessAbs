<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jdblogs.gymlessabs">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.main.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.meal.MealPlanActivity"
            android:parentActivityName=".activities.main.HomeActivity" />
        <activity
            android:name=".activities.workout.FavouritesActivity"
            android:parentActivityName=".activities.main.HomeActivity" />
        <activity
            android:name=".activities.main.SettingsActivity"
            android:parentActivityName=".activities.main.HomeActivity" />
        <activity android:name=".activities.workout.ExercisePlanActivity" />
        <activity android:name=".activities.workout.WorkoutActivity" />
        <activity android:name=".activities.workout.ShuffleWorkoutActivity" />
        <activity android:name=".activities.workout.VideoPlayerActivity" />
        <activity android:name=".activities.workout.SearchActivity" />
        <activity android:name=".activities.workout.StartWorkoutActivity" />
        <activity android:name=".activities.meal.SelectMealDateActivity" />
        <activity android:name=".activities.main.AboutPageActivity"></activity>
    </application>

</manifest>